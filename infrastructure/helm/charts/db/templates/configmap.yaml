apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "db.fullname" . }}-config
  labels:
    {{- include "db.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
