Hi <%= @user.username %>,

To reset your password, click the URL below. The link will expire in 1 hour:

<%= reset_password_url(reset_token: @user.password_reset_token) %>

If you did not request your password to be reset, just ignore this e-mail and your password will stay the same.